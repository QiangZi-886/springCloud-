server:
  port: 8020
spring:
  application:
    name: consumer
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/    #将服务消费者注册到注册中心的这个路径下
  instance:
    prefer-ip-address: true   #将ip也注册进服务中心